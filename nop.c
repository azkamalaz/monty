#include "monty.h"
/**
 * nothing - doesn't do anything.
 * @head: head of the linked list.
 * @ln: line number.
 * Return: nothing.
 */
void nothing(stack_t **head, unsigned int ln)
{
	(void)head;
	(void)ln;
}
