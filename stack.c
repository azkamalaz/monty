#include "monty.h"
/**
 * set_to_stack - sets the format fo the data to a stack.
 * @head: head of the linked list.
 * @ln: line number.
 * Return: nothing.
 */
void set_to_stack(stack_t **head, unsigned int ln)
{
	(void)head;
	(void)ln;

	gvar.s_or_q = 1;
}