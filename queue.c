#include "monty.h"
/**
 * set_to_queue - sets the format of the data to a queue.
 * @head: head of the linked list.
 * @ln: line number.
 * Return: nothing.
 */
void set_to_queue(stack_t **head, unsigned int ln)
{
	(void)head;
	(void)ln;

	gvar.s_or_q = 0;
}
